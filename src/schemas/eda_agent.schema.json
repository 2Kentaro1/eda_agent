{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EDAAgentOutput",
  "type": "object",
  "required": ["role", "version", "content"],
  "properties": {
    "role": {
      "type": "string",
      "enum": ["EDA_AGENT"]
    },
    "version": {
      "type": "string",
      "description": "EDA Agent のスキーマバージョン"
    },
    "content": {
      "type": "object",
      "required": ["code_snippets", "plot_snippets"],
      "properties": {
        "code_snippets": {
          "type": "object",
          "description": "特徴量生成・集計・統計処理などの実行可能な Python コード",
          "additionalProperties": {
            "type": "string",
            "description": "Notebook でそのまま実行可能な Python コード"
          }
        },
        "plot_snippets": {
          "type": "object",
          "description": "可視化用の Python コード（matplotlib / seaborn）",
          "additionalProperties": {
            "type": "string",
            "description": "Notebook でそのまま実行可能な Python コード"
          }
        }
      }
    },
    "metadata": {
      "type": "object",
      "description": "任意のメタ情報（将来拡張用）",
      "additionalProperties": true
    }
  }
}